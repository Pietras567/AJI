npm install -g typescript //installing typescript global if you don't have

To download packages for new project (from project dir):
npm install --save express //installing express
npm install --save-dev @types/express @types/node ts-node //installing definition types for express and node and installing ts-node

To run app:
npx ts-node src/index.ts
